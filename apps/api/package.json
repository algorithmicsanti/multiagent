{
  "name": "@wm/api",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsup src/main.ts --format esm --out-dir dist --no-splitting",
    "dev": "tsx watch src/main.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@wm/agent-core": "workspace:*",
    "@wm/db": "workspace:*",
    "@wm/observability": "workspace:*",
    "bullmq": "^5.30.1",
    "fastify": "^5.2.1",
    "ioredis": "^5.4.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
